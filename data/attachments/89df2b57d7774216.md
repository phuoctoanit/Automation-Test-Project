# Test info

- Name: @submission Automation-Test-Project >> TC 02: User can submit the form with all valid data
- Location: /home/runner/work/Automation-Test-Project/Automation-Test-Project/tests/form/02 - submission.spec.ts:13:9

# Error details

```
Error: The page title is not as expected

Timed out 5000ms waiting for expect(locator).toHaveTitle(expected)

Locator: locator(':root')
Expected string: "DEMOQA"
Received string: "502 Bad Gateway"
Call log:
  - The page title is not as expected with timeout 5000ms
  - waiting for locator(':root')
    9 × locator resolved to <html>…</html>
      - unexpected value "502 Bad Gateway"

    at /home/runner/work/Automation-Test-Project/Automation-Test-Project/tests/form/02 - submission.spec.ts:23:87
    at /home/runner/work/Automation-Test-Project/Automation-Test-Project/tests/form/02 - submission.spec.ts:20:9
```

# Page snapshot

```yaml
- heading "502 Bad Gateway" [level=1]
- separator
- text: nginx/1.17.10 (Ubuntu)
```

# Test source

```ts
   1 | import { test, expect } from './fixtures/shared.fixtures';
   2 | import {FormData} from '../../form-data/formData';
   3 | import * as fs from 'fs';
   4 | import * as path from 'path';
   5 | import { ExcelUtils } from '../../utils/ExcelUtils';
   6 | import { ExcelData } from '../../constants/ExcelData';
   7 | import { Logger } from '../../utils/Logger';
   8 |
   9 | test.describe('@submission Automation-Test-Project', async () => {
  10 |
  11 |     const dataFilePath: string = path.resolve(__dirname, '../../test-data/Data.xlsx');
  12 |
  13 |     test('TC 02: User can submit the form with all valid data', async ({ sharedPage, pageManager }) => {
  14 |
  15 |         const homePage = pageManager.getHomePage();
  16 |
  17 |         const formData = ExcelUtils.parseRowData(
  18 |             path.resolve(__dirname, dataFilePath), ExcelData.SheetName, 1);
  19 |
  20 |         await test.step('1. Navigate to the automation practice form page', async () => {
  21 |             await sharedPage.goto('https://demoqa.com/automation-practice-form'), {timeout: 60000};
  22 |             await sharedPage.waitForURL('**/automation-practice-form', { timeout: 10000 });
> 23 |             await expect(sharedPage, { message: 'The page title is not as expected'}).toHaveTitle('DEMOQA');
     |                                                                                       ^ Error: The page title is not as expected
  24 |         });
  25 |         await test.step('2. Input all valid data into the form', async () => {
  26 |             if (formData.picture !== undefined && formData.picture !== '') {
  27 |                 const filePath = path.resolve(__dirname, formData.picture);
  28 |                 expect(fs.existsSync(filePath)).toBe(true);
  29 |                 formData.picture = filePath;
  30 |             }
  31 |             await homePage.inputForm(formData);
  32 |         });
  33 |         await test.step('3. Validate success modal', async () => {
  34 |             await homePage.validateOnSuccessModal(formData);
  35 |         });
  36 |     });
  37 |
  38 |     test('TC 03: User can submit the form with if input required filed only', async ({ sharedPage, pageManager }) => {
  39 |         const homePage = pageManager.getHomePage();
  40 |         //can use a CSV  or JSON file to store the form data
  41 |         const formData = ExcelUtils.parseRowData(
  42 |             path.resolve(__dirname, dataFilePath), ExcelData.SheetName, 2);
  43 |
  44 |         await test.step('1. Navigate to the automation practice form page', async () => {
  45 |             await sharedPage.goto('https://demoqa.com/automation-practice-form'), {timeout: 60000};
  46 |             await sharedPage.waitForURL('**/automation-practice-form', { timeout: 10000 });
  47 |             await expect(sharedPage).toHaveTitle('DEMOQA');
  48 |         });
  49 |         await test.step('2. Input all valid data into the form', async () => {
  50 |             if (formData.picture !== undefined && formData.picture !== '') {
  51 |                 const filePath = path.resolve(__dirname, formData.picture);
  52 |                 expect(fs.existsSync(filePath)).toBe(true);
  53 |                 formData.picture = filePath;
  54 |             }
  55 |             await homePage.inputForm(formData);
  56 |         });
  57 |         await test.step('3. Validate success modal', async () => {
  58 |             await homePage.validateOnSuccessModal(formData);
  59 |         });
  60 |     });
  61 | });
  62 |
  63 |
```